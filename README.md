# ระบบยืมคืนครุภัณฑ์สำนักงานกองบริหารงานบุคคล

## 1. การเตรียมฐานข้อมูล

1. ตัดสินใจเลือกฐานข้อมูลที่จะใช้งาน เช่น `MySQL` หรือ `PostgreSQL`
2. แก้ไขไฟล์ `.env` ให้ตรงกับข้อมูลของฐานข้อมูล เช่น:
   ```env
   //ยกตัวอย่าง
   #หากเป็นMySQL
   DB_CONNECTION=mysql
   DB_HOST=127.0.0.1
   DB_PORT=3306
   DB_DATABASE=your_database_name
   DB_USERNAME=your_username
   DB_PASSWORD=your_password

   #หากเป็นPostgreSQL
   DB_CONNECTION=pgsql
   DB_HOST=127.0.0.1
   DB_PORT=5432
   DB_DATABASE=project
   DB_USERNAME=postgres
   DB_PASSWORD=csrmu123

   ```
3. รันคำสั่งเพื่อสร้างตารางในฐานข้อมูล:
   ```bash
   php artisan migrate
   ```

---

## 2. การ Seed ข้อมูลเริ่มต้น

ให้ใช้คำสั่งด้านล่างเพื่อสร้างผู้ใช้งานที่เป็นผู้ดูแลระบบ (Admin):

```bash
php artisan db:seed --class=UserSeeder
```

### 🔐 ข้อมูลเข้าสู่ระบบ (Admin)
- **Email**: `adminrmu@example.com`  
- **Password**: `admin2546`

---

## 3. การทดสอบระบบ

1. เริ่มต้นเซิร์ฟเวอร์ Laravel ด้วยคำสั่ง:
   ```bash
   php artisan serve
   ```
2. เปิดเบราว์เซอร์ไปยัง URL ที่ปรากฏใน Terminal เช่น `http://127.0.0.1:8000`
3. เข้าสู่ระบบด้วยรหัสจากข้อ 2

### ⚠️ หากเกิดข้อผิดพลาด โปรดตรวจสอบดังนี้:

- `.env` กำหนดค่าถูกต้องหรือไม่ เช่นชื่อฐานข้อมูล, พอร์ต 
(MySQL`3306` หรือ `3307`, PostgreSQL `5432` หรือ `portอื่นที่ท่านใช้`)
- ตรวจสอบว่า PHP รองรับ MySQL หรือไม่ โดยรัน:
  ```bash
  php -m
  ```
  และดูว่ามี `pdo_mysql,pdo_pgsql` หรือไม่
  ไฟล์php.iniหรือไฟล์configนำ;ออกจากตัวไดรเวอร์ที่ฟ้องerrorออกแล้วหรือไม่
- ตรวจสอบว่า MySQL/PostgreSQL ทำงานอยู่ เช่น บน MAMP/XAMPP หรือใช้ phpMyAdmin
- หากเชื่อมต่อDatabaseไม่ได้ให้ตรวจสอบว่าservice windowตัวนั้นๆเปิดอยู่หรือไม่เช่น 5432 not found    ให้เปิดservice pgsqlก่อน เป็นต้น

---

## 4. ข้อสงวนสิทธิ์

ระบบนี้สงวนสิทธิ์การใช้งานไว้เฉพาะ **กองบริหารงานบุคคล มหาวิทยาลัยราชภัฏมหาสารคาม** เท่านั้น  
ไม่อนุญาตให้นำไปจำหน่ายต่อ

> หากนำไปใช้เพื่อการศึกษา/สอน ให้ขึ้นอยู่กับดุลยพินิจของเจ้าหน้าที่นักวิชาการคอมพิวเตอร์ของกองบริหารงานบุคคล

---

## ผู้จัดทำ

- **ชื่อ**: นายอธิวัฒน์ จันทะศรี  
- **รหัสนักศึกษา**: 653120100116  
- **สาขา**: วิทยาการคอมพิวเตอร์  
- **คณะ**: วิทยาศาสตร์และเทคโนโลยี  
- **มหาวิทยาลัย**: ราชภัฏมหาสารคาม
